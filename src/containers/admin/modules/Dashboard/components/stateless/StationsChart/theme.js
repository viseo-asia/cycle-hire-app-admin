import {white} from "material-ui/styles/colors";

const theme = {
    paper: {
        padding: 20,
        overflow: 'hidden'
    },
    raisedButton: {
        textColor: white,
        backgroundColor: "#283f89"
    }
};

export default theme;