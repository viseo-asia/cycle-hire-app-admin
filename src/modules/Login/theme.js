import { white } from 'material-ui/styles/colors';

const theme = {
    form: {
        fields: {
            color: white
        },
        hintStyle: {
            color: white
        },
        buttons: {
            signin: {
                marginRight: 5
            }
        }
    }
};

export default theme;